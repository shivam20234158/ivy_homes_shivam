DO NOT TRUST API_REFERENCE.md.

The running Ivy Homes API is the source of truth.

Before implementing behavior based on documentation:
1. Reproduce it against the API;
2. Record discrepancies;
3. Prefer observed behavior.

Never invent submission findings.
Never classify a record as corrupt or fake without reproducible evidence.
Never expose the API key in logs, screenshots, frontend bundles, or commits.
